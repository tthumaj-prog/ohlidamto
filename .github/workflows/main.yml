name: Ohlidamto – plánovač

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - name: Test – zpráva do logu
        run: echo "Ohlidamto scheduler is running"

      - name: Test – e-mail (zatím jen simulace)
        run: |
          echo "TEST: Tady bude odeslání e-mailu."
