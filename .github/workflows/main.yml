name: Ohlidamto – plánovač

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Ohlidamto – test notifikace
        run: |
          echo "Ohlidamto běží. Test notifikace."
